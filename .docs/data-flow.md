# MobVibe Data Flow Architecture

> Complete data flow diagrams: Input → Processing → Output for every feature

## Table of Contents
1. [System Overview](#system-overview)
2. [Authentication Flow](#authentication-flow)
3. [Coding Session Flow](#coding-session-flow)
4. [Voice Input Flow](#voice-input-flow)
5. [Icon Generation Flow](#icon-generation-flow)
6. [3D Logo Generation Flow](#3d-logo-generation-flow)
7. [Integrations Flow](#integrations-flow)
8. [Preview Flow](#preview-flow)
9. [Dependencies Matrix](#dependencies-matrix)

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        Mobile App (React Native)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Auth    │  │ Projects │  │ Session  │  │ Settings │      │
│  │  Layer   │  │   List   │  │  (Tabs)  │  │          │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┼─────────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Supabase Edge Functions (API Layer)                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Auth       │  │   Session    │  │   Asset      │        │
│  │   APIs       │  │   APIs       │  │   Proxies    │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
└─────────┼──────────────────┼──────────────────┼────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │ Supabase │      │  Worker  │      │   AI     │
    │   Auth   │      │ Service  │      │ Services │
    │          │      │ (Fly.io) │      │          │
    └──────────┘      └──────────┘      └──────────┘
                            │
                            ▼
                      ┌──────────┐
                      │ Fly.io   │
                      │ Sandbox  │
                      │ (Claude) │
                      └──────────┘
```

**Key Layers:**
- **Presentation**: React Native mobile app (iOS/Android)
- **API Gateway**: Supabase Edge Functions (auth, validation, proxying)
- **Orchestration**: Worker Service (long-running tasks)
- **Execution**: Fly.io Sandboxes (Claude Agent coding)
- **Storage**: Supabase (Postgres + Storage + Realtime)
- **External**: AI services (Claude, Nano Banana, Meshy, ElevenLabs)

---

## Authentication Flow

### Input
- **Source**: User interaction (mobile app)
- **Data**: Email, OAuth provider selection
- **Trigger**: Login/Signup screen buttons

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: User Action (Mobile)                                    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Email: user@example.com                                        │
│ • Method: "magic-link" | "google" | "apple"                     │
│ • Device: Platform info, push token                             │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Supabase Auth (Server)                                  │
│ Location: Supabase Auth service                                 │
│ Process:                                                         │
│ 1. Validate email format                                        │
│ 2. Check if user exists                                         │
│ 3. Generate magic link OR initiate OAuth flow                   │
│ 4. Send email OR redirect to provider                           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: User Verification (External)                            │
│ • Magic Link: User clicks email link                            │
│ • OAuth: User authorizes on provider site                       │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Session Creation (Server)                               │
│ Location: Supabase Auth                                         │
│ Process:                                                         │
│ 1. Verify callback token                                        │
│ 2. Create user record (if new)                                  │
│ 3. Generate JWT access token                                    │
│ 4. Generate refresh token                                       │
│ 5. Create profile in profiles table                             │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • access_token: "eyJhbGc..."                                     │
│ • refresh_token: "v1.Mk..."                                      │
│ • user: { id, email, metadata }                                 │
│ • profile: { tier, sessions_used, sessions_limit }              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Client State Update (Mobile)                            │
│ Location: Mobile app memory                                     │
│ Process:                                                         │
│ 1. Store tokens in secure storage                               │
│ 2. Set auth state in Zustand store                              │
│ 3. Navigate to Projects List                                    │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: Mobile app state + secure storage
- **Data**: JWT tokens, user profile
- **Result**: Authenticated session, navigation to home

### Dependencies
- **Requires**: Internet connection, valid email/OAuth provider
- **Blocks**: All protected features (coding sessions, projects)
- **Persists**: Tokens in SecureStore, session in Supabase

---

## Coding Session Flow

### Input
- **Source**: User prompt (text or voice)
- **Data**: Natural language description of app
- **Trigger**: "Start Building" button

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Prompt Submission (Mobile)                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • prompt: "Build a todo app with categories and due dates"      │
│ • projectId: "uuid-123"                                          │
│ • userId: "uuid-456"                                             │
│ • voiceFile?: base64 audio (if voice input)                     │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Session Creation (Edge Function)                        │
│ Location: supabase/functions/start-coding-session               │
│ Duration: <5 seconds                                             │
│ Process:                                                         │
│ 1. Validate JWT token → Extract user_id                         │
│ 2. Check profile.sessions_used < sessions_limit                 │
│ 3. Increment sessions_used counter                              │
│ 4. Create coding_sessions record:                               │
│    • id, user_id, project_id, prompt                            │
│    • status: "pending"                                           │
│    • expires_at: NOW() + 30 minutes                             │
│ 5. Create coding_jobs record:                                   │
│    • session_id, prompt, status: "queued"                       │
│    • priority: tier === "pro" ? 1 : 2                           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • sessionId: "uuid-789"                                          │
│ • wsUrl: "wss://...supabase.co/sessions/uuid-789"               │
│ • status: "pending"                                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: WebSocket Connection (Mobile)                           │
│ Location: Mobile app                                             │
│ Process:                                                         │
│ 1. Connect to Supabase Realtime channel                         │
│ 2. Subscribe to session-specific events                         │
│ 3. Display "Connecting..." status                               │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Job Polling (Worker Service)                            │
│ Location: Worker Service (Fly.io/Render/Railway)                │
│ Duration: Continuous polling                                     │
│ Process:                                                         │
│ 1. Poll coding_jobs WHERE status = "queued"                     │
│ 2. ORDER BY priority ASC, created_at ASC                        │
│ 3. Pick next job, mark status = "processing"                    │
│ 4. Record started_at timestamp                                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Sandbox Creation (Worker Service)                       │
│ Location: Worker Service                                         │
│ Duration: 2-5 seconds                                            │
│ Process:                                                         │
│ 1. Call Fly.io API to create microVM                            │
│    • Image: mobvibe-sandbox:latest                              │
│    • Resources: 1 vCPU, 2GB RAM                                 │
│    • Env: PROJECT_ID, SESSION_ID                                │
│ 2. Wait for sandbox to be ready                                 │
│ 3. Initialize /workspace directory                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 6: Claude Agent Initialization (Worker Service)            │
│ Location: Worker Service                                         │
│ Process:                                                         │
│ 1. Create Anthropic client                                      │
│ 2. Load system prompt (React Native expert)                     │
│ 3. Configure tools (bash, filesystem, expo)                     │
│ 4. Prepare conversation with user prompt                        │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 7: Claude Coding Loop (Worker + Sandbox)                   │
│ Location: Worker Service orchestrates, Sandbox executes         │
│ Duration: 5-30 minutes                                           │
│ Process:                                                         │
│ Loop until Claude says "done":                                   │
│   1. Claude thinks → Emit "thinking" event                      │
│   2. Claude uses tool (bash/write_file/etc)                     │
│   3. Tool executes in sandbox                                   │
│   4. Emit event to Realtime:                                    │
│      • "terminal" for bash commands                             │
│      • "file_change" for file operations                        │
│   5. Tool result returns to Claude                              │
│   6. Claude continues or finishes                               │
│                                                                  │
│ Events emitted:                                                  │
│ • thinking: Claude's reasoning                                  │
│ • terminal: $ npm install zustand                               │
│ • file_change: Created app/index.tsx                            │
│ • preview_ready: EAS Update URL                                 │
│ • completion: "Todo app ready!"                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 8: Preview Publishing (Sandbox)                            │
│ Location: Sandbox                                                │
│ Duration: 20-30 seconds                                          │
│ Process:                                                         │
│ 1. Claude runs: eas update --branch preview-{sessionId}         │
│ 2. EAS CLI uploads bundle to Expo servers                       │
│ 3. EAS returns preview URL                                      │
│ 4. Emit preview_ready event with URL                            │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 9: File Storage (Worker Service)                           │
│ Location: Worker Service                                         │
│ Process:                                                         │
│ For each file created:                                           │
│ 1. Upload to Supabase Storage:                                  │
│    • Bucket: project-files                                      │
│    • Path: {projectId}/{path}                                   │
│ 2. Insert metadata to project_files table:                      │
│    • path, storage_path, file_type, size_bytes                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 10: Cleanup (Worker Service)                               │
│ Location: Worker Service                                         │
│ Process:                                                         │
│ 1. Update coding_sessions status = "completed"                  │
│ 2. Update coding_jobs status = "completed"                      │
│ 3. Record completed_at timestamp                                │
│ 4. Destroy Fly.io sandbox (stop billing)                        │
│ 5. Emit final completion event                                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • Generated files in Supabase Storage                           │
│ • File metadata in project_files table                          │
│ • Preview URL for WebView                                       │
│ • agent_events history for debugging                            │
│ • Updated session status                                        │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: Supabase Storage, Mobile app preview
- **Data**: Source code files, preview URL, event history
- **Result**: Working React Native app, viewable in-app

### Dependencies
- **Requires**: Valid auth token, session quota available, Worker Service operational, Fly.io sandbox capacity
- **Blocks**: Preview tab (until preview_ready), Code download (until files uploaded)
- **Persists**: Files in Storage, metadata in Postgres, events for history

---

## Voice Input Flow

### Input
- **Source**: User holds microphone button
- **Data**: Raw audio recording
- **Trigger**: Long press on voice button

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Recording Start (Mobile)                                │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • User Action: Long press on 🎤 button                          │
│ • Permission: Microphone access granted                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Audio Capture (Mobile - Native)                         │
│ Location: React Native (expo-speech-recognition)                │
│ Duration: While button held (max 60s)                           │
│ Process:                                                         │
│ 1. Request microphone permission                                │
│ 2. Start native speech recognizer                               │
│    • iOS: SFSpeechRecognizer                                    │
│    • Android: SpeechRecognizer                                  │
│ 3. Stream audio to recognizer                                   │
│ 4. Display waveform animation                                   │
│ 5. Show "Listening..." indicator                                │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Real-Time Transcription (Device)                        │
│ Location: On-device (iOS/Android native API)                    │
│ Duration: Real-time as user speaks                              │
│ Process:                                                         │
│ 1. Native API transcribes audio → text                          │
│ 2. Partial results returned in real-time                        │
│ 3. Display partial text to user                                 │
│ 4. User releases button → Finalize                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Post-Processing (Mobile)                                │
│ Location: Mobile app                                             │
│ Process:                                                         │
│ 1. Trim whitespace from transcript                              │
│ 2. Capitalize first letter                                      │
│ 3. Add period if missing                                        │
│ 4. Insert into prompt input field                               │
│ 5. Enable "Start Building" button                               │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • transcript: "Build a fitness tracker with meal logging"       │
│ • confidence: 0.95 (high accuracy)                              │
│ • duration: 3.2 seconds                                          │
│ • UI State: Prompt field populated, button enabled              │
└─────────────────────────────────────────────────────────────────┘
```

**Alternative: Cloud-Based Transcription (Fallback)**

```
┌─────────────────────────────────────────────────────────────────┐
│ If native recognition unavailable or low confidence:            │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2B: Cloud Transcription (Edge Function)                    │
│ Location: supabase/functions/transcribe-audio                   │
│ Process:                                                         │
│ 1. Upload audio to temp storage                                 │
│ 2. Call Google Cloud Speech-to-Text API                         │
│ 3. Receive transcript                                            │
│ 4. Delete temp audio file                                       │
│ 5. Return transcript to mobile                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: Prompt input field (mobile UI)
- **Data**: Transcribed text
- **Result**: User can review/edit before submitting

### Dependencies
- **Requires**: Microphone permission, internet (for cloud fallback)
- **Blocks**: None (optional enhancement to text input)
- **Persists**: Nothing (transcript ephemeral until prompt submitted)

---

## Icon Generation Flow

### Input
- **Source**: User text prompt in Icon Gen tab
- **Data**: Icon description, style preference
- **Trigger**: "Generate Icon" button

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Icon Request (Mobile)                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • prompt: "Minimalist fitness dumbbell with progress circle"    │
│ • style: "modern" | "minimal" | "bold"                          │
│ • projectId: "uuid-123"                                          │
│ • iterations: 3 (generate 3 variations)                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Rate Limiting Check (Edge Function)                     │
│ Location: supabase/functions/generate-icon                      │
│ Process:                                                         │
│ 1. Validate JWT → Extract user_id                               │
│ 2. Check usage table:                                            │
│    • SELECT count(*) FROM icon_generations                      │
│    • WHERE user_id = ? AND created_at > NOW() - INTERVAL '1hr' │
│ 3. Compare against tier limit:                                  │
│    • Free: 5/hour, Starter: 20/hour, Pro: 50/hour              │
│ 4. If exceeded → Return 429 Rate Limit                          │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Generate Variations (Edge Function)                     │
│ Location: Edge Function                                          │
│ Duration: 3-8 seconds per variation                             │
│ Process:                                                         │
│ For i in [1, 2, 3]:                                              │
│   1. Call Nano Banana API:                                      │
│      POST https://api.nanobanana.com/v1/generate                │
│      Headers: Authorization: Bearer <server_key>                │
│      Body: {                                                     │
│        prompt: "{prompt} variation {i}",                        │
│        size: "1024x1024",                                        │
│        format: "png",                                            │
│        style: "{style}"                                          │
│      }                                                           │
│   2. Receive imageUrl from Nano Banana                          │
│   3. Download image data                                        │
│   4. Upload to Supabase Storage:                                │
│      • Bucket: project-files                                    │
│      • Path: {projectId}/icons/icon-{timestamp}-v{i}.png        │
│   5. Insert to icon_generations table:                          │
│      • user_id, project_id, prompt, storage_path                │
│      • style, iteration: i                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Generate Signed URLs (Edge Function)                    │
│ Location: Edge Function                                          │
│ Process:                                                         │
│ For each uploaded icon:                                          │
│ 1. Call supabase.storage.createSignedUrl()                      │
│ 2. Expiry: 3600 seconds (1 hour)                                │
│ 3. Collect all signed URLs                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • variations: [                                                  │
│     {                                                            │
│       id: "uuid-1",                                              │
│       url: "https://...supabase.co/storage/...?token=...",      │
│       iteration: 1                                               │
│     },                                                           │
│     { id: "uuid-2", url: "...", iteration: 2 },                │
│     { id: "uuid-3", url: "...", iteration: 3 }                 │
│   ]                                                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Display Gallery (Mobile)                                │
│ Location: Icon Gen tab                                           │
│ Process:                                                         │
│ 1. Render 3 variations in grid                                  │
│ 2. User taps favorite → Highlight selection                     │
│ 3. User taps "Apply to Project"                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 6: Apply Icon (Mobile → Edge Function)                     │
│ Location: supabase/functions/apply-icon                         │
│ Process:                                                         │
│ 1. Copy selected icon to canonical location:                    │
│    • From: {projectId}/icons/icon-{timestamp}-v2.png            │
│    • To: {projectId}/app-icon.png                               │
│ 2. Update projects table:                                       │
│    • SET icon_url = new_signed_url                              │
│ 3. Notify Claude (if session active):                           │
│    • Update expo config with new icon path                      │
│    • Regenerate app.json with icon reference                    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 7: Code Integration (Worker Service)                       │
│ Location: Worker Service (if session active)                    │
│ Process:                                                         │
│ 1. Update app.json in sandbox:                                  │
│    {                                                             │
│      "expo": {                                                   │
│        "icon": "./assets/icon.png",                             │
│        "adaptiveIcon": { ... }                                  │
│      }                                                           │
│    }                                                             │
│ 2. Download icon from Storage                                   │
│ 3. Write to sandbox: /workspace/assets/icon.png                 │
│ 4. Emit file_change event                                       │
│ 5. Re-publish EAS Update with new icon                          │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: Supabase Storage, Project config, Mobile preview
- **Data**: 3 icon variations, selected icon applied to project
- **Result**: App uses custom icon in preview and builds

### Dependencies
- **Requires**: Valid auth, rate limit quota, Nano Banana API access
- **Blocks**: None (optional, app works without custom icon)
- **Persists**: Icons in Storage, selection in projects table, config in code

---

## 3D Logo Generation Flow

### Input
- **Source**: User text prompt in Icon Gen tab (3D mode)
- **Data**: Logo description, 3D style preference
- **Trigger**: "Generate 3D Logo" button

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: 3D Logo Request (Mobile)                                │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • prompt: "Modern tech startup logo with circuit pattern"       │
│ • style3d: "low-poly" | "realistic" | "stylized"               │
│ • format: "glb" | "fbx" (3D model formats)                      │
│ • projectId: "uuid-123"                                          │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Service Selection (Edge Function)                       │
│ Location: supabase/functions/generate-3d-logo                   │
│ Process:                                                         │
│ 1. Check user tier:                                              │
│    • Free: Luma AI ($1/capture)                                 │
│    • Pro: Meshy AI (monthly credits)                            │
│ 2. Route to appropriate service                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Option A: Meshy AI (Pro Users)                                  │
│ Location: Edge Function                                          │
│ Duration: 30-90 seconds                                          │
│ Process:                                                         │
│ 1. Call Meshy API:                                               │
│    POST https://api.meshy.ai/v1/text-to-3d                      │
│    Headers: Authorization: Bearer <server_key>                  │
│    Body: {                                                       │
│      prompt: "{prompt}",                                         │
│      art_style: "{style3d}",                                     │
│      output_format: "glb"                                        │
│    }                                                             │
│ 2. Poll for completion (task_id)                                │
│ 3. Download .glb file                                            │
│ 4. Convert to PNG renderings (front, side, top views)          │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Option B: Luma AI (Free Users)                                  │
│ Location: Edge Function                                          │
│ Duration: 20-60 seconds                                          │
│ Process:                                                         │
│ 1. Call Luma Genie API:                                          │
│    POST https://api.lumalabs.ai/v1/genie                        │
│    Headers: Authorization: Bearer <server_key>                  │
│    Body: { prompt: "{prompt}" }                                 │
│ 2. Receive 3D model URL                                          │
│ 3. Download model                                                │
│ 4. Render to PNG images                                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Storage & Metadata (Edge Function)                      │
│ Location: Edge Function                                          │
│ Process:                                                         │
│ 1. Upload 3D model to Storage:                                  │
│    • Path: {projectId}/3d-logos/logo-{timestamp}.glb            │
│ 2. Upload PNG renderings:                                        │
│    • {projectId}/3d-logos/logo-{timestamp}-front.png            │
│    • {projectId}/3d-logos/logo-{timestamp}-side.png             │
│ 3. Insert to logo_3d_generations table:                         │
│    • user_id, project_id, prompt, model_path, preview_paths     │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • model_url: "https://...supabase.co/.../logo.glb"              │
│ • previews: [front.png, side.png, top.png]                      │
│ • thumbnail: front.png (for 2D icon conversion)                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: 2D Icon Conversion (Edge Function)                      │
│ Location: Edge Function (optional step)                          │
│ Process:                                                         │
│ 1. Take front-facing PNG render                                 │
│ 2. Resize to 1024x1024                                           │
│ 3. Apply circular mask (for iOS)                                │
│ 4. Generate adaptive icon layers (Android)                      │
│ 5. Save as app-icon.png                                         │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: Supabase Storage, Icon Gen tab preview
- **Data**: 3D model (.glb), 2D renderings (PNG), converted app icon
- **Result**: User can preview 3D logo, convert to 2D app icon

### Dependencies
- **Requires**: Valid auth, Pro tier (for Meshy) or pay-per-use (Luma), 3D rendering capability
- **Blocks**: None (optional premium feature)
- **Persists**: 3D models in Storage, metadata in logo_3d_generations table

---

## Integrations Flow

### Overview
Each integration follows similar pattern: Connect → Configure → Apply to Code

### Sound Generation Integration

```
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Integration: "Sounds"                                          │
│ • Action: "Add UI sound effects"                                │
│ • Sounds requested: ["tap", "success", "error", "notification"] │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Sound Generation (Edge Function)                        │
│ Location: supabase/functions/generate-sounds                    │
│ Process:                                                         │
│ For each sound type:                                             │
│ 1. Call ElevenLabs Sound Effects API:                           │
│    POST https://api.elevenlabs.io/v1/sound-generation           │
│    Body: {                                                       │
│      text: "short {sound_type} sound effect",                   │
│      duration_seconds: 0.5,                                      │
│      prompt_influence: 0.8                                       │
│    }                                                             │
│ 2. Download audio buffer (MP3)                                  │
│ 3. Upload to Storage:                                            │
│    • Path: {projectId}/sounds/{sound_type}.mp3                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Code Integration (Worker Service)                       │
│ Location: Worker Service (Claude)                                │
│ Process:                                                         │
│ Claude automatically:                                            │
│ 1. Install expo-av: npm install expo-av                         │
│ 2. Create sounds utility:                                        │
│    // lib/sounds.ts                                              │
│    import { Audio } from 'expo-av';                             │
│    export const playSound = (type) => { ... }                   │
│ 3. Add sound calls to UI interactions:                          │
│    <Button onPress={() => {                                      │
│      playSound('tap');                                           │
│      handlePress();                                              │
│    }} />                                                         │
│ 4. Emit file_change events for sound files                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • Sound files in Storage and project assets/                    │
│ • expo-av installed and configured                              │
│ • Sound utility functions in codebase                           │
│ • UI components playing sounds on interaction                   │
└─────────────────────────────────────────────────────────────────┘
```

### Haptics Integration

```
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Integration: "Haptics"                                         │
│ • Patterns: ["light", "medium", "heavy", "success", "error"]   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Haptics Setup (Worker Service)                          │
│ Location: Worker Service (Claude)                                │
│ Process:                                                         │
│ Claude automatically:                                            │
│ 1. Install expo-haptics (included in Expo)                      │
│ 2. Create haptics utility:                                       │
│    // lib/haptics.ts                                             │
│    import * as Haptics from 'expo-haptics';                     │
│    export const haptic = {                                       │
│      light: () => Haptics.impactAsync('light'),                 │
│      success: () => Haptics.notificationAsync('success')        │
│    }                                                             │
│ 3. Add to key interactions:                                      │
│    • Button press → light                                        │
│    • Success action → success                                    │
│    • Error → error                                               │
│    • Long press → medium                                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • Haptics configured in app                                     │
│ • Utility functions added                                        │
│ • UI components trigger haptics                                 │
│ • No external API needed (native iOS/Android)                   │
└─────────────────────────────────────────────────────────────────┘
```

### Supabase Backend Integration

```
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Integration: "Supabase Backend"                               │
│ • Features: ["Database", "Auth", "Storage"]                     │
│ • User provides: Supabase URL, Anon Key                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Store Credentials (Edge Function)                       │
│ Location: supabase/functions/add-integration                    │
│ Process:                                                         │
│ 1. Encrypt Supabase keys                                        │
│ 2. Store in integrations table:                                 │
│    • user_id, project_id, type: "supabase"                      │
│    • encrypted_config: { url, anon_key }                        │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Code Generation (Worker Service)                        │
│ Location: Worker Service (Claude)                                │
│ Process:                                                         │
│ Claude automatically:                                            │
│ 1. Install @supabase/supabase-js                                │
│ 2. Create .env file:                                             │
│    EXPO_PUBLIC_SUPABASE_URL=https://...                         │
│    EXPO_PUBLIC_SUPABASE_ANON_KEY=...                            │
│ 3. Create Supabase client:                                       │
│    // lib/supabase.ts                                            │
│    export const supabase = createClient(url, key)               │
│ 4. Generate data hooks:                                          │
│    // hooks/useData.ts                                           │
│    export function useTodos() {                                  │
│      return useQuery(['todos'], async () => {                   │
│        const { data } = await supabase.from('todos').select()   │
│        return data                                               │
│      })                                                          │
│    }                                                             │
│ 5. Integrate into UI components                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • Supabase client configured                                    │
│ • Database queries working                                       │
│ • Auth flows integrated                                          │
│ • Storage upload/download implemented                           │
└─────────────────────────────────────────────────────────────────┘
```

### Stripe Payment Integration

```
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Integration: "Stripe Payments"                                │
│ • Products: [{ name: "Premium", price: 999 }]                   │
│ • User provides: Stripe Publishable Key                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Backend Setup (Edge Function)                           │
│ Location: supabase/functions/stripe-webhook                     │
│ Process:                                                         │
│ 1. Store Stripe keys (encrypted)                                │
│ 2. Create Stripe products via API                               │
│ 3. Setup webhook endpoint                                        │
│ 4. Generate checkout session creator function                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Mobile Integration (Worker Service)                     │
│ Location: Worker Service (Claude)                                │
│ Process:                                                         │
│ Claude generates:                                                │
│ 1. Payment screen with product cards                            │
│ 2. Stripe checkout WebView integration                          │
│ 3. Success/failure handling                                      │
│ 4. Subscription status checking                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • Stripe checkout flows working                                 │
│ • Payment screen in app                                          │
│ • Subscription status synced                                     │
│ • Webhook handling purchases                                     │
└─────────────────────────────────────────────────────────────────┘
```

### GitHub Integration

```
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • Integration: "GitHub"                                          │
│ • Action: "Save source code to GitHub"                          │
│ • User authorizes GitHub OAuth                                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: OAuth Flow (Edge Function + GitHub)                     │
│ Location: supabase/functions/github-oauth                       │
│ Process:                                                         │
│ 1. Redirect to GitHub OAuth                                     │
│ 2. User authorizes MobVibe app                                  │
│ 3. Receive OAuth token                                           │
│ 4. Store encrypted token in integrations table                  │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Repository Creation (Edge Function)                     │
│ Location: supabase/functions/create-github-repo                 │
│ Process:                                                         │
│ 1. Call GitHub API to create repo:                              │
│    POST https://api.github.com/user/repos                       │
│    Body: {                                                       │
│      name: "{project_name}",                                     │
│      description: "Built with MobVibe",                         │
│      private: true                                               │
│    }                                                             │
│ 2. Initialize with README                                        │
│ 3. Create .gitignore for React Native                           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Code Push (Edge Function)                               │
│ Location: Edge Function                                          │
│ Process:                                                         │
│ 1. Download all files from Supabase Storage                     │
│ 2. Create Git tree structure                                     │
│ 3. Commit all files via GitHub API:                             │
│    POST /repos/{owner}/{repo}/git/blobs (for each file)         │
│    POST /repos/{owner}/{repo}/git/trees                         │
│    POST /repos/{owner}/{repo}/git/commits                       │
│    PATCH /repos/{owner}/{repo}/git/refs/heads/main             │
│ 4. Update projects table with repo URL                          │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Output:                                                          │
│ • GitHub repo created with all source code                      │
│ • User owns the repo (can clone, modify, deploy)               │
│ • Repo URL stored in project metadata                           │
│ • Future syncs can push updates                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Preview Flow

### Input
- **Source**: User taps Preview tab
- **Data**: Session ID, preview URL (from preview_ready event)
- **Trigger**: Tab selection

### Processing

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Tab Switch (Mobile)                                     │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Input:                                                           │
│ • sessionId: "uuid-789"                                          │
│ • previewUrl: "https://u.expo.dev/update/..."                   │
│ • expiresAt: timestamp (60 minutes from generation)             │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: URL Validation (Mobile)                                 │
│ Location: Mobile app                                             │
│ Process:                                                         │
│ 1. Check if previewUrl exists                                   │
│ 2. Check if expired (expiresAt < NOW())                         │
│ 3. If expired → Show "Generate New Preview" button              │
│ 4. If valid → Proceed to load                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: WebView Load (Mobile)                                   │
│ Location: Preview tab (react-native-webview)                    │
│ Duration: 2-5 seconds initial load                              │
│ Process:                                                         │
│ 1. Initialize WebView component                                 │
│ 2. Set source: { uri: previewUrl }                              │
│ 3. Display loading indicator                                    │
│ 4. WebView fetches bundle from EAS CDN                          │
│ 5. JavaScript bundle loads and executes                         │
│ 6. React Native app renders in WebView                          │
│ 7. Hide loading indicator                                       │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: User Interaction (Mobile)                               │
│ Location: WebView (running React Native app)                    │
│ Process:                                                         │
│ • User can interact with app fully                              │
│ • Tap buttons, fill forms, navigate screens                     │
│ • Test features like data persistence, animations               │
│ • WebView acts as Expo Go container                             │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Auto-Refresh on Updates (Mobile)                        │
│ Location: Preview tab                                            │
│ Process:                                                         │
│ 1. Listen for preview_ready events                              │
│ 2. If new preview URL received:                                 │
│    • Update previewUrl state                                    │
│    • WebView auto-reloads (useEffect)                           │
│    • User sees updated app instantly                            │
└─────────────────────────────────────────────────────────────────┘
```

### Output
- **Destination**: WebView in Preview tab
- **Data**: Rendered React Native app (interactive)
- **Result**: User tests app functionality in-app

### Dependencies
- **Requires**: Valid preview URL, EAS Update published, internet connection
- **Blocks**: None (can switch to other tabs)
- **Persists**: Preview URL in session state, WebView state in memory

---

## Dependencies Matrix

### Component Dependencies

```
┌────────────────────────────────────────────────────────────────────┐
│ Component Dependency Graph                                         │
└────────────────────────────────────────────────────────────────────┘

Authentication Layer
├─ Required by: ALL protected features
├─ Provides: JWT tokens, user_id, profile data
└─ Blocks: Projects List, Coding Sessions, Integrations, Settings

Projects List
├─ Depends on: Authentication
├─ Provides: Project selection for sessions
└─ Blocks: Coding Session creation

Coding Session
├─ Depends on: Authentication, Projects List, Worker Service
├─ Provides: Generated code, preview URL, file metadata
└─ Blocks: Preview tab (until preview_ready), Code download

Voice Input
├─ Depends on: Microphone permission
├─ Optional: Does NOT block text input
└─ Enhances: Prompt input experience

Icon Generation
├─ Depends on: Authentication, Nano Banana API
├─ Optional: Does NOT block app generation
└─ Enhances: App branding

3D Logo Generation
├─ Depends on: Authentication, Meshy/Luma API, Pro tier (optional)
├─ Optional: Does NOT block app generation
└─ Enhances: Premium branding experience

Integrations
├─ Depends on: Active coding session, third-party credentials
├─ Optional: Does NOT block app generation
└─ Enhances: App functionality (backend, payments, etc.)

Preview Tab
├─ Depends on: Coding Session completion, EAS Update URL
├─ Blocked by: No preview URL available
└─ Enables: In-app testing

Worker Service
├─ Depends on: Coding job in queue, Fly.io capacity
├─ Required by: All coding sessions
└─ Provides: Code generation orchestration

Sandboxes (Fly.io)
├─ Depends on: Worker Service, Fly.io API
├─ Required by: Claude code execution
└─ Provides: Isolated execution environment

Supabase Realtime
├─ Depends on: Active session, internet connection
├─ Required by: Real-time event streaming
└─ Provides: Live updates to mobile app
```

### Page Connection Map

```
┌────────────────────────────────────────────────────────────────────┐
│ Screen Navigation & Data Flow                                      │
└────────────────────────────────────────────────────────────────────┘

Login/Signup Screen
      │
      ├─→ Email/OAuth flow
      │
      ▼
Projects List (Home)
      │
      ├─→ Tap project → Project Details
      ├─→ Tap + New → New Project Setup
      │
      ▼
New Project Setup
      │
      ├─→ Enter prompt (text or voice)
      ├─→ Select template (optional)
      │
      ▼
Coding Session Screen (Bottom Tabs)
      │
      ├─→ Code Tab (default)
      │   ├─ Terminal output
      │   ├─ File tree
      │   └─ Code viewer
      │
      ├─→ Preview Tab
      │   ├─ WebView with app
      │   └─ Reload/Screenshot controls
      │
      ├─→ Integrations Tab
      │   ├─ Backend services
      │   ├─ Payments
      │   ├─ Auth providers
      │   ├─ Sounds
      │   ├─ Haptics
      │   └─ GitHub
      │
      └─→ Icon Gen Tab
          ├─ 2D icons (Nano Banana)
          ├─ 3D logos (Meshy/Luma)
          └─ Icon gallery & apply

Hamburger Menu (☰)
      │
      ├─→ Settings
      ├─→ Profile
      ├─→ Usage & Billing
      └─→ Logout

Data Flow Between Screens:
• Projects List → Coding Session: projectId
• Coding Session → Preview: previewUrl, sessionId
• Icon Gen → Code Tab: selected icon path
• Integrations → Code Tab: config changes trigger re-generation
• All screens → Hamburger Menu: user profile data
```

### Backend Dependencies

```
┌────────────────────────────────────────────────────────────────────┐
│ Backend Service Dependencies                                        │
└────────────────────────────────────────────────────────────────────┘

Supabase (Core Infrastructure)
├─ Auth: User authentication & authorization
├─ Postgres: Relational data (users, projects, sessions, jobs)
├─ Storage: Object storage (code files, assets, icons)
├─ Realtime: WebSocket event streaming
└─ Edge Functions: API gateway & proxies

Worker Service (Long-Running Tasks)
├─ Depends on: Supabase (job queue), Fly.io (sandboxes), Anthropic (Claude)
├─ Provides: Code generation orchestration
└─ Managed by: Fly.io / Render / Railway

Fly.io (Sandboxes)
├─ Depends on: Worker Service commands
├─ Provides: Isolated Docker containers for Claude
└─ Lifecycle: Created per session, destroyed after completion

External APIs (AI Services)
├─ Anthropic Claude: Code generation (critical)
├─ Nano Banana: 2D icon generation (optional)
├─ Meshy AI: 3D logo generation (optional, Pro only)
├─ Luma AI: 3D logo generation (optional, Free alternative)
├─ ElevenLabs: Sound generation (optional)
├─ Google Speech-to-Text: Voice transcription fallback (optional)
└─ All proxied through Edge Functions for security

Third-Party Integrations (User-Configured)
├─ Supabase: User's own backend
├─ Stripe: User's payment processing
├─ GitHub: User's code repository
└─ Credentials stored encrypted in integrations table
```

---

## Cost Analysis Per Flow

### Coding Session
- **Claude API**: ~$0.45 per session (input + output tokens)
- **Fly.io Sandbox**: ~$0.0045 per session (15 min avg)
- **EAS Update**: Free (included in Expo)
- **Supabase**: Negligible (included in plan)
- **Total**: ~$0.45 per session

### Icon Generation
- **Nano Banana**: ~$0.02 per icon (3 variations = $0.06)
- **Supabase Storage**: Negligible
- **Total**: ~$0.06 per generation

### 3D Logo Generation
- **Meshy AI (Pro)**: ~$0.02 per logo (from monthly credits)
- **Luma AI (Free/Pay-per-use)**: ~$1.00 per logo
- **Supabase Storage**: Negligible
- **Total**: $0.02 (Pro) or $1.00 (Free tier)

### Voice Input
- **Native Recognition (iOS/Android)**: Free
- **Google Cloud Speech-to-Text (fallback)**: ~$0.006 per 15 seconds
- **Total**: Free (native) or ~$0.006 (cloud)

### Sound Generation
- **ElevenLabs**: ~$0.05 per sound (4 sounds = $0.20)
- **Total**: ~$0.20 per integration

---

**Status**: Comprehensive data flow documentation complete ✅ | All inputs/processing/outputs defined ✅ | Dependencies mapped ✅
