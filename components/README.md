# Components

Reusable React Native components for MobVibe.

## Structure

- `ui/` - Basic UI components (buttons, inputs, cards)
- `features/` - Feature-specific components (code viewer, file tree, etc.)
- `layout/` - Layout components (headers, containers)
