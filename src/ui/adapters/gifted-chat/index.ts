/**
 * Gifted Chat Adapter
 *
 * Re-exports the centralized Chat component wrapper.
 * Provides complete chat UI with MobVibe token theming.
 */

export { Chat } from '../../components/Chat';
export type { ChatProps, IMessage, User } from '../../components/Chat';
