# Constants

Design tokens, colors, typography, and other constants for MobVibe.

## Structure

- `colors.ts` - Color palette
- `typography.ts` - Font styles
- `spacing.ts` - Spacing scale
- `config.ts` - App configuration
